import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-bim-calculator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
